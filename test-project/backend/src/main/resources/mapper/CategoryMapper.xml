<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CategoryMapper">
    <select id="findAll" resultType="com.example.model.Category">
        SELECT * FROM categorys
    </select>
    <select id="findById" resultType="com.example.model.Category">
        SELECT * FROM categorys WHERE id = #{id}
    </select>
    <insert id="insert" parameterType="com.example.model.Category">
        INSERT INTO categorys (name, created_at) VALUES (#{name}, NOW())
    </insert>
    <update id="update">
        UPDATE categorys SET name = #{name} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM categorys WHERE id = #{id}
    </delete>
</mapper>